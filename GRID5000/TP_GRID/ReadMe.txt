
ReadMe Pour Grid5000


Utilisation du grid 5000

POur lancer le calcul de pi, il faut lancer une réservation passive puis lancer le script python deplay_tree.py.

Il utilise une représentation des machines sous forme d'arbre pour accélerer le déploiement du script.


quelque remarque pendant le tp:

pour se connecter aller dans le dossier .ssh et taper la comande : socksify ssh -i id_rsa.pub iafrass@access.grid5000.fr
taper le mot de passe du fil.

pour se connecter à le grid de lille.
taper mot de passe de grid.

var = "$var2" //var retourne la valeur de var2
var = '$var2' // var = $var2 comme chaine
car = `ls`    // var = le resultat de ls.

echo $var // pour afficher la valeur de var.
